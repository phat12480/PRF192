#include <stdio.h>

int main (){
	int a,b;
	double quotient, modulea, moduleb;
	printf("\nnhap a va b");
	scanf("%d %d", &a, &b);
	printf("\na va b la %d %d", a,b);
if (a%2==0){
		printf	("\na la chan");
} 	else	printf ("\na la le");

return 0;
}
